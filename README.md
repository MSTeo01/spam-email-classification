# Spam Email Classifier

This classifier distinguishes spam (junk or commercial or bulk) emails from ham (non-spam) emails with around 94% accuracy.

This project utilizes: feature engineering with text data(using NLP practices), sklearn libraries to process data and fit models, cross validation to minimize overfitting, generating and analyzing precision-recall curves.
